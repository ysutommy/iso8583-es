/**
 * 域信息定义
 *
 * 用JSON格式定义结构会更清晰，性能更好。但为了方便整理查看各域定义信息，就定义成如下格式了。
 * 而且只需初始化一次，对性能影响很小
 *
 * 具体应用可参考此配置修改
 *
 * @type {string}
 */
declare const FieldDefinition: string;
export default FieldDefinition;
